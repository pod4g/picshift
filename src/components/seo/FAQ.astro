---
import { getUI } from '../../i18n';
import type { Locale } from '../../i18n/config';

interface Props {
  faqs: Array<{ q: string; a: string }>;
  lang?: Locale;
}

const { faqs, lang = 'en' } = Astro.props;
const ui = getUI(lang);

const jsonLd = {
  '@context': 'https://schema.org',
  '@type': 'FAQPage',
  mainEntity: faqs.map((faq) => ({
    '@type': 'Question',
    name: faq.q,
    acceptedAnswer: {
      '@type': 'Answer',
      text: faq.a,
    },
  })),
};
---

<section class="mt-8 sm:mt-12">
  <h2 class="text-xl sm:text-2xl font-bold text-text-primary mb-4 sm:mb-6">
    {ui.faqTitle}
  </h2>
  <div class="space-y-3">
    {faqs.map((faq) => (
      <details class="group bg-card-bg rounded-xl border border-border overflow-hidden">
        <summary class="flex items-center justify-between gap-3 px-4 py-3 sm:px-5 sm:py-4 cursor-pointer list-none text-sm sm:text-base text-text-primary font-semibold hover:bg-drop-bg/50 transition-colors">
          <span>{faq.q}</span>
          <svg
            class="w-5 h-5 flex-shrink-0 text-text-secondary transition-transform group-open:rotate-180"
            fill="none"
            stroke="currentColor"
            viewBox="0 0 24 24"
          >
            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M19 9l-7 7-7-7" />
          </svg>
        </summary>
        <div class="px-4 pb-3 sm:px-5 sm:pb-4 text-sm sm:text-base text-text-secondary leading-relaxed">
          {faq.a}
        </div>
      </details>
    ))}
  </div>
</section>

<script type="application/ld+json" set:html={JSON.stringify(jsonLd)} />
